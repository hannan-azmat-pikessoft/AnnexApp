import { DEEPLINK_BASE } from "../utility/constants";

const config = {
    screens: {

    },
};

const linking = {
    prefixes: [`${DEEPLINK_BASE}://`],
    config: config,
};

export default linking;